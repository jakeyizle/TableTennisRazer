@page
@model TableTennisRazer.Pages.Matches.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Match[0].WinningScore)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Match[0].LosingScore)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Match) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WinningScore)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LosingScore)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.MatchID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.MatchID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.MatchID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
